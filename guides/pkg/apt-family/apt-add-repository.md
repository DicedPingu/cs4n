# `apt-add-repository` cheat sheet
Adds a repository into the /etc/apt/sources.list or .

## Syntax
- Primary usage: `sudo apt-add-repository [subcommand/options] [targets]`
- Use `--help` before destructive actions and prefer simulation flags where available.

## Expanded Cheats (2026-03)
More command/subcommand/option examples you can run directly.

| Command | Use case |
| --- | --- |
| `tldr add-apt-repository` | View documentation for the original command |
| `apt-add-repository --help` | Show command usage and option reference |
| `man apt-add-repository` | Open the full manual page |
| `whatis apt-add-repository` | Print one-line command description |
| `type -a apt-add-repository` | Show how the shell resolves the command path |
| `command -v apt-add-repository` | Print executable path used by current shell |
| `tldr apt-add-repository` | Show concise command examples |
| `apropos apt-add-repository` | Search manual database for related commands |
| `apt-add-repository --version` | Print local tool version |
