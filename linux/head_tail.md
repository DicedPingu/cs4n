# head / tail — view start or end of files
# Use it for logs and large files.

- `head -n 20 file` — first 20 lines
- `tail -n 20 file` — last 20 lines
- `tail -f file` — follow appending
- `tail -F file` — follow with log rotation
