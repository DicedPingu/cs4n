# cat — print and combine files
# Use it to view or concatenate files quickly.

- `cat file` — print file
- `cat -n file` — show line numbers
- `cat -A file` — show hidden chars
- `cat a b > out` — concatenate
- `tac file` — reverse order
