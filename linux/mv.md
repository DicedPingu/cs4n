# mv — move or rename files
# Use it to move, rename, or overwrite files.

- `mv src dest` — move/rename
- `mv -i src dest` — confirm overwrite
- `mv -n src dest` — no overwrite
- `mv -v src dest` — verbose
