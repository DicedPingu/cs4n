# fwupd — firmware update tooling
# Use it to check and apply device firmware updates.

- `fwupdmgr get-devices` — list devices
- `fwupdmgr refresh` — update metadata
- `fwupdmgr get-updates` — available updates
- `fwupdmgr update` — apply updates (needs sudo)
